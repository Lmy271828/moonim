// moonim — a mathematical animation engine kernel in MoonBit.
// Learn more about moon.mod configuration:
// https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html

name = "Lmy271828/moonim"

version = "0.2.0"

readme = "README.md"

repository = "https://github.com/Lmy271828/moonim"

license = "Apache-2.0"

keywords = [ "animation", "geometry", "bezier", "math", "svg" ]

preferred_target = "wasm"

description = "A mathematical animation engine kernel: bezier geometry, mobject scene graph, typed animation combinators and an SVG backend."
