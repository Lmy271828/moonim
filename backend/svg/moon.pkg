// Package: SVG backend — serialize a mobject tree at any instant to an
// SVG document. Pure computation, so it runs in CI and golden tests.

import {
  "lmy271828/moonim/math",
  "lmy271828/moonim/geom",
  "lmy271828/moonim/mobject",
}
